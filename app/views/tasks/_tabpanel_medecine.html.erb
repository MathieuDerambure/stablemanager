<div class="col-xs-12">
  <h3> <strong>💊 Médicaments </strong> </h3>
  <div class="line"></div>
</div>
<% @tasks.each do |task| %>

  <% if task.done == false %>

    <% if task.doing == false %>
      <% if !task.medecine.nil? %>
          <div class="col-xs-12">
            <div class="task-message" data-task-id="<%= task.id %>">
              <% if task.horse.photo? %>
                <div class="horse-avatar" style="background-image:  url('<%= cl_image_path task.horse.photo, width: 300, crop: :fill %>')">
                </div>
              <%else %>
                <div class="horse-avatar" style="background-image:  url('<%= image_path("default-horse.jpg")  %>')">
                </div>
              <% end %>
              <div class="task-message-body">
                <p><strong><%= task.horse.name %></strong> Box n° <%= task.horse.box %></p>
                <p class="text-primary">

                  <%= task.medecine&.name %>
                </p>

              </div>
              <%= link_to mark_as_doing_path(task_id: task.id), remote: true do %>
                <%= image_tag "user-default.png", class: "avatar-large"%>
              <% end %>

            </div>
          </div>
      <% end %>
    <% end %>

    <% if task.doing == true %>
      <% if !task.medecine.nil? %>
        <div class="col-xs-12">
          <div class="task-message" data-task-id="<%= task.id %>">
            <% if task.horse.photo? %>
              <div class="horse-avatar" style="background-image:  url('<%= cl_image_path task.horse.photo, width: 300, crop: :fill %>')">
              </div>
            <%else %>
              <div class="horse-avatar" style="background-image:  url('<%= image_path("default-horse.jpg")  %>')">
              </div>
            <% end %>
            <div class="task-message-body">
              <p><strong><%= task.horse.name %></strong> Box n° <%= task.horse.box %></p>
              <p class="text-primary">
                <%= task.medecine&.name %>
              </p>
              <%= link_to "Terminé !", mark_as_done_path(task_id: task.id), remote: true, class: "btn btn-default" %>
            </div>
          </div>
        </div>
      <% end %>
    <% end %>

  <% end %>

<% end %>
