<div class="col-xs-12">
  <h3> <strong>🐎 Activités </strong></h3>
  <div class="line"></div>
</div>

<% @tasks.each do |task| %>
  <% if task.done == false && !task.activity.nil? %>
    <div class="col-xs-12">
      <%= render "tasks/activity/task", task: task %>
    </div>
  <% end %>
<% end %>
