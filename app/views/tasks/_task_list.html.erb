<% if tasks.any? %>
  <div class="task-message-body">
    <p>
      <strong><%= horse.name %> - Box n°<%= horse.box %></strong>
    </p>
      <% tasks.each do |task| %>
        <p>
          <%= task.food_type&.name %>
          <% if task.food_quantity %>
            |<strong><%= task.food_quantity%> L</strong>
          <% end %>
        </p>

      <% end %>

  </div>
  <!-- TODO tasks -->
  <% if tasks.where(doing: false, done: false).any? %>
    <%= link_to "S'assigner", mark_as_doing_food_horse_tasks_path(horse, time: time), method: :patch, class: 'btn btn-default' %>
  <!-- ON GOING tasks -->
  <% elsif tasks.where(doing: true, done: false).any? %>

    <%= link_to mark_as_done_food_horse_tasks_path(horse, time: time), method: :patch do%>
      <% image_link = tasks.first.user_doing&.picture || "https://res.cloudinary.com/dk6vsgttl/image/upload/v1535978381/pomme.png" %>
      <%= image_tag image_link, class: "avatar-large" %>
    <% end %>
  <% end %>
<% end %>
<%#= time.capitalize %>
<%#= task.id %>
<%#= task.start_time %>

