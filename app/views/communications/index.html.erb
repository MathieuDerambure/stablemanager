<div class="container">
  <div class="row" >
    <div class="col-xs-8">
      <% @comms.each do |comm| %>
      <p> <%= comm.id %>. <%= comm.created_at.strftime("Le %d %b à %H:%M") %> - <%= comm.message %> </p>
      <% end %>
    </div>
  </div>
</div>
