<%= simple_form_for (@comm) do |f| %>
  <%= f.input :message, label: false, placeholder: 'message...' %>
  <%= f.submit "Ajouter un message", class: "btn btn-lg btn-primary btn-block" %>
  <%= link_to "", communications_path, class: "btn btn-lg btn-secondary btn-block"%>
<% end %>
