<%= simple_form_for (@comm) do |f| %>
  <%= f.input :message, label: false, placeholder: '...' %>
  <%= f.submit "Ajouter votre message", class: "btn btn-xs btn-primary btn-block" %>
  <%= link_to "", communications_path, class: "btn btn-xs btn-secondary btn-block"%>
<% end %>
